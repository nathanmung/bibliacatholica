import { bibleBooks } from './lib/bibleData';
import Link from 'next/link';

export default function Home() {
  // Pour MVP, tout Ancien (Genèse seul) ; ajoute Nouveau plus tard
  const ancienBooks = bibleBooks.filter(b => b.testament === 'Ancien');

  return (
    <main className="min-h-screen bg-white dark:bg-gray-900 text-gray-900 dark:text-white p-4">
      <h1 className="text-3xl font-bold text-center mb-8">Bible Catholique Française</h1>
      <section className="space-y-8">
        <div>
          <h2 className="text-2xl font-semibold mb-4">Ancien Testament</h2>
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
            {ancienBooks.map(book => (
              <Link key={book.id} href={`/bible/${book.id}/1`} className="p-4 bg-gray-100 dark:bg-gray-800 rounded-lg shadow hover:shadow-lg transition">
                <h3 className="text-xl font-medium">{book.name}</h3>
              </Link>
            ))}
          </div>
        </div>
        {/* Ajoute section Nouveau Testament plus tard quand on scrape plus */}
      </section>
    </main>
  );
}